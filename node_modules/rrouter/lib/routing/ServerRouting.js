/**
 * @jsx React.DOM
 */
'use strict';

var Routing     = require('./Routing');
var matchRoutes = require('../matchRoutes');

for(var Routing____Key in Routing){if(Routing.hasOwnProperty(Routing____Key)){ServerRouting[Routing____Key]=Routing[Routing____Key];}}var ____SuperProtoOfRouting=Routing===null?null:Routing.prototype;ServerRouting.prototype=Object.create(____SuperProtoOfRouting);ServerRouting.prototype.constructor=ServerRouting;ServerRouting.__superConstructor__=Routing;
  function ServerRouting(routes) {
    this.routes = routes;
    this.match = undefined;
  }

  ServerRouting.prototype.update=function(path, query) {
    this.match = matchRoutes(this.routes, path, query);

    return this.match;
  };


module.exports = ServerRouting;
